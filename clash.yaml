port: 7890
socks-port: 7891
allow-lan: true
mode: Rule
log-level: info

proxy-providers:
  magic:
    type: http
    url: "https://raw.githubusercontent.com/freefq/free/master/v2" # 自动抓取源
    path: ./magic.yaml
    interval: 3600
    health-check:
      enable: true
      url: http://www.gstatic.com/generate_204
      interval: 300

proxy-groups:
  - name: 🚀 自动选择最快
    type: url-test   # <--- 这就是你想要的自动筛选代码！
    url: 'http://www.gstatic.com/generate_204'
    interval: 300
    use:
      - magic

rules:
  - GEOIP,CN,DIRECT
  - MATCH,🚀 自动选择最快