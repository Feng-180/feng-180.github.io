port: 7890
socks-port: 7891
allow-lan: true
mode: Rule
log-level: info

# 自动从远程获取上百个节点
proxy-providers:
  magic:
    type: http
    url: "https://raw.githubusercontent.com/freefq/free/master/v2"
    path: ./magic.yaml
    interval: 3600
    health-check:
      enable: true
      url: http://www.gstatic.com/generate_204
      interval: 300

proxy-groups:
  - name: 🚀 魔法枢纽
    type: url-test   # 自动选路：谁快选谁
    url: 'http://www.gstatic.com/generate_204'
    interval: 300
    use:
      - magic

rules:
  - GEOIP,CN,DIRECT
  - MATCH,🚀 魔法枢纽